apiVersion: v1
kind: Pod
metadata:
  name: trestlebot-e2e-pod
  labels:
    app: trestlebot-e2e
spec:
  containers:
  - name: mock-server-container
    image: localhost/mock-server:latest
    ports:
    - containerPort: 8080
